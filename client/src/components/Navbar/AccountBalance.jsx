//import {zilliqa,userAddressn, units,BN, getAddressFromPrivateKey, decryptPrivateKey, userAddress}  from '@zilliqa-js/account';



/*let keystore = JSON.parse(this.state.encryptedWallet);
  const pk = decryptPrivateKey(this.state.passphrase, keystore);
  const address = getAddressFromPrivateKey(pk);*/


/*const { Zilliqa } = require('@zilliqa-js/zilliqa');
const zilliqa = new Zilliqa('https://dev-api.zilliqa.com');
let balanceState = zilliqa.blockchain.getBalance(userAddress);
if (balanceState) {
  let balance = balanceState.result.balance;
  balance = units.fromQa(new BN(balance), units.Units.Zil); // user's $zil balance
}*/


/*
!!Above have issues. run and press f12!!

Navbar.jsx - balanceState

Zilpay login happens stright away when joining site, not one clickEvent

Home link in logo

clear footer

start page contents

get Json for products (even use a test one if needed)

Css need cleaning up.

Background Images

Product Images

Add testing for

*/
  
  
 